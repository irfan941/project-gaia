version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: gaia
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - gaia_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: ./backend/.env
    environment:
      - DATABASE_URL=postgresql+psycopg://postgres:password@db:5432/gaia
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - ./memory:/memory
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  gaia_pgdata:
